# 首次设置（只需执行一次）

git init
git add .
git commit -m "初始化基金查询工具"
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main


# 日常更新（每次修改后执行）

git add .
git commit -m "更新说明"
git push


# 注意事项

1. 先在 GitHub 创建仓库
2. 替换上面的"你的用户名"和"仓库名"
3. 推送时密码使用 Personal Access Token，不是账号密码
